import styled from "styled-components"
import MidShelf from "./Shelves/MidShelf"

const Entrance = () => {
  return (
      <MidShelf />
  )
}

const VerticalShelf = styled.div`
  
`

const HorizontalShelf = styled.div`
  
`

export default Entrance